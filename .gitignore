# build artifacts
node_modules/
dist/
.build/
*.tsbuildinfo

# NOTE: bin/ovp IS committed on purpose — plugin install runs with --ignore-scripts, so the
# engine cannot be built during install. Shipping the universal binary keeps
# `openclaw plugins install clawhub:...` a one-liner (see README).

# local research reports contain real screen content (private) — never publish
RECON.md
PHASE1_REPORT.md
PHASE1_5_REPORT.md
PHASE2_REPORT.md
docs/private/

# test scratch
/tmp/
*.png
*.jpg

# packaging artifact
*.tgz

# macOS
.DS_Store
